# Cursor API Dashboard — пример переменных окружения
# Скопируйте в .env и задайте нужные значения (файл .env не коммитится в git).

# Сервер
PORT=3333
DATA_DIR=./data

# Cursor Admin API (можно также задать ключ в веб-интерфейсе «Настройки и загрузка»)
# CURSOR_API_KEY=

# Безопасность и CORS
# CORS_ORIGIN=http://localhost:3333
# SESSION_SECRET=

# Прокси и лимиты
# PROXY_TIMEOUT_MS=60000
# RATE_LIMIT_MAX=120
# REQUEST_DELAY_MS=150

# Логи
# SYNC_LOG_FILE=./data/sync.log

# Парсинг PDF-счетов (OpenDataLoader). Требуется Java 11+ в PATH.
# Включено по умолчанию; отключить: USE_OPENDATALOADER=0 или false
# USE_OPENDATALOADER=1
# OPENDATALOADER_TABLE_METHOD=default
# OPENDATALOADER_USE_STRUCT_TREE=0
# INVOICE_LOGS_DIR=./data/invoice-logs

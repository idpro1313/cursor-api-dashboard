# Встроенное логирование

## ✅ Автоматическое логирование

Приложение **автоматически** пишет все логи в файл без дополнительных настроек!

### Что логируется

- ✅ Все `console.log()` и `console.error()`
- ✅ Все запросы к эндпоинтам
- ✅ Все ошибки с полным стеком
- ✅ Детальная информация о каждом запросе (с requestId)

### Где находятся логи

#### Локально (без Docker):
```
./data/logs/app.log
```

#### В Docker:
- **Контейнер**: `/data/logs/app.log`
- **Хост (Linux)**: `/opt/cursor/data/logs/app.log`
- **Хост (Windows)**: `C:\cursor\data\logs\app.log`

## 🚀 Просмотр логов

### Способ 1: Прямой просмотр файла

```bash
# Linux/Mac
tail -f /opt/cursor/data/logs/app.log

# Windows PowerShell
Get-Content C:\cursor\data\logs\app.log -Wait -Tail 50
```

### Способ 2: Docker логи (тоже работают)

```bash
docker compose logs -f app
```

### Способ 3: Скрипты (если нужны фильтры)

```bash
# Linux/Mac
./scripts/view-logs.sh -f           # Следить за логами
./scripts/view-logs.sh -e           # Только ошибки
./scripts/analyze-logs.sh           # Анализ последней ошибки

# Windows PowerShell
.\scripts\view-logs.ps1 -Follow
.\scripts\view-logs.ps1 -Error
.\scripts\analyze-logs.ps1
```

## 📊 Структура логов

Каждая строка логов - это JSON или текст, готовый для анализа:

```
[APP] Starting Cursor API Dashboard
[APP] Data directory: /data
[APP] Log file: /data/logs/app.log
[ACTIVITY-BY-MONTH] REQUEST_START {"requestId":"abc123","startDate":"2025-06-01",...}
[ACTIVITY-BY-MONTH] PARAMS_PARSED {"requestId":"abc123",...}
[DB] getAnalytics CALL {"options":{...}}
[ACTIVITY-BY-MONTH] RESPONSE_SENT {"requestId":"abc123"}
```

### При ошибке:

```
[ACTIVITY-BY-MONTH] ERROR {"requestId":"abc123","errorMessage":"Cannot read properties of undefined","errorStack":"..."}
```

## 🔍 Поиск ошибок

### Последние ошибки:
```bash
grep ERROR /opt/cursor/data/logs/app.log | tail -20
```

### Логи конкретного запроса:
```bash
grep '"requestId":"abc123"' /opt/cursor/data/logs/app.log
```

### Статистика:
```bash
# Сколько запросов
grep -c REQUEST_START /opt/cursor/data/logs/app.log

# Сколько ошибок
grep -c ERROR /opt/cursor/data/logs/app.log

# Сколько успешных ответов
grep -c RESPONSE_SENT /opt/cursor/data/logs/app.log
```

## 🛠️ Настройка (опционально)

### Изменить путь к логам

Через переменную окружения:

```bash
# Linux/Mac
export DATA_DIR=/custom/path
node server.js

# Docker compose
environment:
  - DATA_DIR=/custom/path
```

### Отключить логирование в файл

Если по какой-то причине нужно отключить:

В `server.js` закомментируйте раздел:
```javascript
// fs.appendFile(LOG_FILE, logLine, function(err) {
//   ...
// });
```

## 📁 Ротация логов

Логи накапливаются постоянно. Настройте ротацию:

### Linux (logrotate):

```bash
sudo nano /etc/logrotate.d/cursor-api-dashboard
```

Содержимое:
```
/opt/cursor/data/logs/app.log {
    daily
    rotate 14
    compress
    delaycompress
    notifempty
    missingok
    copytruncate
}
```

### Ручная очистка:

```bash
# Очистить файл
> /opt/cursor/data/logs/app.log

# Или удалить старый лог
rm /opt/cursor/data/logs/app.log
# Приложение создаст новый автоматически
```

## 🔄 После обновления

После `git pull` и перезапуска:
1. ✅ Логи продолжают писаться в тот же файл
2. ✅ Не нужна дополнительная настройка
3. ✅ Работает и в Docker, и локально

## 🎯 Производительность

- **Запись асинхронная** - не блокирует приложение
- **Неблокирующая I/O** - используется `fs.appendFile`
- **Минимальные накладные расходы** - только перехват console.log

## ❓ FAQ

### Логи не создаются?

Проверьте права:
```bash
ls -la /opt/cursor/data/logs/
```

Должна быть возможность записи.

### Лог-файл слишком большой?

Настройте ротацию (см. выше) или очистите вручную:
```bash
> /opt/cursor/data/logs/app.log
```

### Логи пишутся только в консоль?

Проверьте, что директория существует и доступна для записи:
```bash
mkdir -p /opt/cursor/data/logs
chmod 755 /opt/cursor/data/logs
```

### Нужны только Docker логи?

Docker логи тоже работают:
```bash
docker compose logs -f app
```

Они содержат ту же информацию, что и файл.

## 📝 Примеры использования

### Отладка ошибки на сервере:

```bash
# 1. Воспроизведите ошибку в браузере
# 2. На сервере найдите последнюю ошибку
cd /opt/cursor/cursor-api-dashboard
./scripts/analyze-logs.sh

# 3. Результат в /tmp/cursor-last-error.log
cat /tmp/cursor-last-error.log
```

### Мониторинг в реальном времени:

```bash
# Следить за логами
tail -f /opt/cursor/data/logs/app.log

# Только ошибки
tail -f /opt/cursor/data/logs/app.log | grep ERROR

# Только запросы к API
tail -f /opt/cursor/data/logs/app.log | grep ACTIVITY-BY-MONTH
```

### Экспорт для анализа:

```bash
# Последние 1000 строк
tail -1000 /opt/cursor/data/logs/app.log > /tmp/recent.log

# Все логи за сегодня
grep "$(date +%Y-%m-%d)" /opt/cursor/data/logs/app.log > /tmp/today.log

# Скопировать на локальную машину
scp user@server:/tmp/today.log ./
```

## ✨ Преимущества встроенного логирования

- ✅ **Работает из коробки** - не нужна настройка
- ✅ **Автоматически** - включается при старте
- ✅ **Всегда доступно** - и в Docker, и локально
- ✅ **Не требует внешних инструментов** - только Node.js
- ✅ **Готово для анализа ИИ** - структурированный JSON формат
- ✅ **Совместимо с Docker** - логи доступны и через `docker logs`
